# innigkeit

build options:

- target architecture (x86_64, aarch64, maybe riscv64)
- target bootloader (limine, since we're doing limine protocol)
- target bios (uefi only, version 6)
- disk layout (gpt only)
